<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Câmera: necessária para CameraX capturar frames ao vivo -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Internet: publicação MQTT das detecções -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Armazenamento: leitura/escrita de imagens no armazenamento interno do app -->
    <!-- No Android 10+, o acesso ao diretório privado do app não precisa de permissão -->
    <!-- Abaixo do Android 10, precisamos da permissão de escrita para storage externo -->
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <!-- Hardware: indica que o app requer câmera traseira -->
    <uses-feature android:name="android.hardware.camera" android:required="true" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />

    <application
        android:name=".PlacaScanApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:windowSoftInputMode="adjustResize"
        android:theme="@style/Theme.PlacaScan">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
